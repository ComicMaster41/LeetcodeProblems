In this problem, you consider a vector of elem type int and determine the max distance between two different integers. The brute-force approach, which I implemented, was just two for loops and you 
calculate the distance if two elements differed. I think something worth discussing was I considered using a stack at the beginning but I don't think that's a good approach because we aren't necessarily
building a vector less than we are finding the max distance.
